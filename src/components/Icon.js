import * as React from 'react';
import { createComponent } from '@lit-labs/react';
import { Icon } from '@refinitiv-ui/elements/icon';

export default createComponent({
  tagName: 'ef-icon',
  elementClass: Icon,
  react: React,
});
